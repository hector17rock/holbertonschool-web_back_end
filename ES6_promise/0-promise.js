export default function getResponseFromAPI() {
  return new Promise((resolve) => {
    // This is a basic Promise that can be resolved or rejected
    // For this exercise, we just need to return a Promise instance
    resolve();
  });
}
